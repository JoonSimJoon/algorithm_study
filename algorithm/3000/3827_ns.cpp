#include <stdio.h>
#include <algorithm>
//acmicpc.net/problem/3827

int N, M, A, B, C, T;
int main() {
	while (1) {
		scanf("%d %d %d %d %d %d", &N, &M, &A, &B, &C, &T);
		if (N == 0) break;
		else {

		}
	}
	return 0;
}	