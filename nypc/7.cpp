#include <iostream>
#include <string.h>
#include <vector>
using namespace std;
#define ll long long

ll N;
ll arr[300010];
vector<ll> v[300010];
int main(){
	ios::sync_with_stdio(false);
	cin.tie(false);
	cout.tie(false);
	cin>>N;
	memset(arr,-1,sizeof(arr));
	for(ll a,b,c,i=1;i<N;i++){
		cin>>a>>b>>c;
		
	}
	return 0;
}